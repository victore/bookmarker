{{#each model as |user|}}
  {{!-- <p>{{user.firstName}} {{user.lastName}}</p> --}}
  <p>{{user.fullName}}</p>
  <ul>
    {{#each user.bookmarks as |bookmark|}}
      <li>{{bookmark.name}}</li>
    {{/each}}
  </ul>
{{/each}}
{{outlet}}